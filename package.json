{
  "name": "pulse-prism",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@playwright/test": "^1.58.2",
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
